# SF_INTEGRATE library
file(GLOB SF_INTEGRATE_LOCAL_SRC
  "GAUSS_QUADRATURE.f90"
  "SF_INTEGRATE.f90"
  )

include_directories(include)

SET(SF_INTEGRATE_src ${SF_INTEGRATE_LOCAL_SRC})
ADD_LIBRARY(SF_INTEGRATELIB OBJECT ${SF_INTEGRATE_src})
